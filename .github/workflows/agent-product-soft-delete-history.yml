name: Agent validate product soft-delete history

on:
  push:
    branches:
      - agent/product-soft-delete-history
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout technical branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Rebuild clean commits
        shell: bash
        run: |
          set -euo pipefail
          cp .agent-tools/apply_product_soft_delete.py /tmp/apply_product_soft_delete.py
          cp .agent-tools/patch_product_history_test.py /tmp/patch_product_history_test.py
          git config user.name "adminfpestoque"
          git config user.email "adminfpestoque@users.noreply.github.com"
          git fetch origin main
          git checkout -B agent/product-soft-delete-ready origin/main
          python /tmp/apply_product_soft_delete.py
          python /tmp/patch_product_history_test.py

          git add backend/inventory
          git commit -m "feat: keep deleted products in historical records"

          git add frontend/src/pages/products.jsx frontend/src/pages/documents.jsx
          git commit -m "feat: show deleted products as historical rows"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        run: python -m pip install -r backend/requirements.txt

      - name: Check Django migrations
        working-directory: backend
        env:
          USE_SQLITE_FOR_TESTS: "true"
        run: python manage.py makemigrations --check --dry-run

      - name: Run backend tests
        working-directory: backend
        shell: bash
        run: |
          set -euo pipefail
          if ! python manage.py test > /tmp/backend-tests.log 2>&1; then
            tail -n 220 /tmp/backend-tests.log > ../backend-test-failure.txt
            cd ..
            git add backend-test-failure.txt
            git commit -m "chore: capture backend test failure"
            git push origin HEAD:agent/product-soft-delete-diagnostic --force
            cat backend-test-failure.txt
            exit 1
          fi
          tail -n 40 /tmp/backend-tests.log

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Publish validated commits
        shell: bash
        run: |
          set -euo pipefail
          git push origin agent/product-soft-delete-ready --force
