import { React, useEffect, useMemo, useState, api, unwrap, fmtMoney, fmtQty, fmtDate, today, getError, Logo, Button, Modal, Toast, Field, EmptyState, Pagination, DataTable, StatusBadge, AlertTriangle, Archive, ArrowDownToLine, ArrowUpFromLine, BarChart3, Bell, Boxes, Check, ChevronDown, CircleDollarSign, ClipboardCheck, Eye, EyeOff, FileDown, FileText, Gauge, History, Layers3, LogOut, Menu, Package, Pencil, Plus, RefreshCw, Search, Settings, ShieldCheck, SlidersHorizontal, Trash2, Truck, UserCog, Users, Warehouse, X, Bar, BarChart, CartesianGrid, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "../shared.jsx";
import { PageHeader } from "../layout.jsx";
import { useList, SearchBar } from "./listing.jsx";
export function CategoriesPage({ notify, me }) {
  const list = useList("categories/"); const [form, setForm] = useState(null);
  async function save(e) { e.preventDefault(); try { if (form.id) await api.put(`categories/${form.id}/`, form); else await api.post("categories/", form); notify("Categoria salva."); setForm(null); list.reload(); } catch (err) { notify(getError(err), "error"); } }
  return <><PageHeader title="Categorias" description="Organize os produtos por grupos." actions={me.permissions.is_admin && <Button icon={Plus} onClick={() => setForm({ name: "", description: "", active: true })}>Nova categoria</Button>} /><section className="panel"><DataTable loading={list.loading} rows={list.rows} columns={[{ key: "name", label: "Categoria" }, { key: "description", label: "Descrição" }, { key: "active", label: "Situação", render: (r) => <StatusBadge value={r.active ? "active" : "inactive"} label={r.active ? "Ativa" : "Inativa"} /> }, { key: "actions", label: "Ações", render: (r) => me.permissions.is_admin ? <button className="icon-btn" onClick={() => setForm({ ...r })}><Pencil size={16} /></button> : "-" }]} /></section>{form && <Modal title="Categoria" onClose={() => setForm(null)}><form className="form-grid" onSubmit={save}><Field label="Nome" required><input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} required /></Field><Field label="Descrição"><textarea value={form.description || ""} onChange={(e) => setForm({ ...form, description: e.target.value })} /></Field><Field label="Situação"><select value={String(form.active)} onChange={(e) => setForm({ ...form, active: e.target.value === "true" })}><option value="true">Ativa</option><option value="false">Inativa</option></select></Field><div className="form-actions full"><Button>Salvar</Button></div></form></Modal>}</>;
}
