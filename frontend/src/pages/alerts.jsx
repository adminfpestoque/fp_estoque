import { React, useEffect, useMemo, useState, api, unwrap, fmtMoney, fmtQty, fmtDate, today, getError, Logo, Button, Modal, Toast, Field, EmptyState, Pagination, DataTable, StatusBadge, AlertTriangle, Archive, ArrowDownToLine, ArrowUpFromLine, BarChart3, Bell, Boxes, Check, ChevronDown, CircleDollarSign, ClipboardCheck, Eye, EyeOff, FileDown, FileText, Gauge, History, Layers3, LogOut, Menu, Package, Pencil, Plus, RefreshCw, Search, Settings, ShieldCheck, SlidersHorizontal, Trash2, Truck, UserCog, Users, Warehouse, X, Bar, BarChart, CartesianGrid, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "../shared.jsx";
import { PageHeader } from "../layout.jsx";
import { useList } from "./listing.jsx";
export function AlertsPage({ me, notify }) {
  const list = useList("alerts/", { active: true });
  async function refresh() { try { const r = await api.post("alerts/refresh/"); notify(`${r.data.created} alertas recalculados.`); list.reload(); } catch (err) { notify(getError(err), "error"); } }
  return <><PageHeader title="Central de alertas" description="Estoque mínimo, falta de produtos, validade e divergências." actions={me.permissions.is_admin && <Button icon={RefreshCw} onClick={refresh}>Recalcular alertas</Button>} /><div className="filters-bar"><select value={list.params.type || ""} onChange={(e) => list.setParams({ ...list.params, type: e.target.value, page: 1 })}><option value="">Todos os tipos</option><option value="LOW_STOCK">Estoque baixo</option><option value="OUT_OF_STOCK">Sem estoque</option><option value="EXPIRING">Próximo do vencimento</option><option value="EXPIRED">Vencido</option><option value="INVENTORY_DIVERGENCE">Divergência de inventário</option></select><select value={list.params.level || ""} onChange={(e) => list.setParams({ ...list.params, level: e.target.value, page: 1 })}><option value="">Todos os níveis</option><option value="INFO">Informativo</option><option value="WARNING">Atenção</option><option value="CRITICAL">Crítico</option></select></div><section className="panel"><DataTable loading={list.loading} rows={list.rows} columns={[{ key: "level_display", label: "Nível", render: (r) => <StatusBadge value={r.level} label={r.level_display} /> }, { key: "type_display", label: "Tipo" }, { key: "product_name", label: "Produto" }, { key: "lot_number", label: "Lote" }, { key: "message", label: "Mensagem" }, { key: "created_at", label: "Detectado em", render: (r) => fmtDate(r.created_at) }]} /></section></>;
}
