import { React, useEffect, useMemo, useState, api, unwrap, fmtMoney, fmtQty, fmtDate, today, getError, Logo, Button, Modal, Toast, Field, EmptyState, Pagination, DataTable, StatusBadge, AlertTriangle, Archive, ArrowDownToLine, ArrowUpFromLine, BarChart3, Bell, Boxes, Check, ChevronDown, CircleDollarSign, ClipboardCheck, Eye, EyeOff, FileDown, FileText, Gauge, History, Layers3, LogOut, Menu, Package, Pencil, Plus, RefreshCw, Search, Settings, ShieldCheck, SlidersHorizontal, Trash2, Truck, UserCog, Users, Warehouse, X, Bar, BarChart, CartesianGrid, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "../shared.jsx";
import { PageHeader } from "../layout.jsx";
import { useList } from "./listing.jsx";
export function SettingsPage({ notify }) {
  const list = useList("settings/"); const [form, setForm] = useState(null);
  async function save(e) { e.preventDefault(); try { if (form.id) await api.put(`settings/${form.key}/`, form); else await api.post("settings/", form); notify("Configuração salva."); setForm(null); list.reload(); } catch (err) { notify(getError(err), "error"); } }
  return <><PageHeader title="Configurações" description="Parâmetros administrativos do estoque e dos alertas." actions={<Button icon={Plus} onClick={() => setForm({ key: "", value: "", description: "" })}>Nova configuração</Button>} /><section className="panel"><DataTable rows={list.rows} loading={list.loading} columns={[{ key: "key", label: "Chave" }, { key: "value", label: "Valor" }, { key: "description", label: "Descrição" }, { key: "actions", label: "Ações", render: (r) => <button className="icon-btn" onClick={() => setForm({ ...r })}><Pencil size={16} /></button> }]} /></section>{form && <Modal title="Configuração" onClose={() => setForm(null)}><form className="form-grid" onSubmit={save}><Field label="Chave" required><input disabled={!!form.id} value={form.key} onChange={(e) => setForm({ ...form, key: e.target.value })} required /></Field><Field label="Valor" required><input value={form.value} onChange={(e) => setForm({ ...form, value: e.target.value })} required /></Field><Field label="Descrição"><textarea value={form.description || ""} onChange={(e) => setForm({ ...form, description: e.target.value })} /></Field><div className="form-actions full"><Button>Salvar</Button></div></form></Modal>}</>;
}
